# Vite Plus plugin
# https://viteplus.dev
# Enable Vite Plus shell completions and environment when installed.
export VITE_PLUS_HOME="$HOME/.vite-plus"

[ -f "$VITE_PLUS_HOME/env" ] && source "$VITE_PLUS_HOME/env"
